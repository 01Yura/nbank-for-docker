# 🔧 Настройка GitHub Pages для отчетов

## Проблема

Если пайплайн проходит успешно, но отчеты недоступны по адресу `https://01yura.github.io/nbank-for-docker/`, это означает, что **GitHub Pages не включен** в настройках репозитория.

## Решение

### Шаг 1: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub: `https://github.com/01Yura/nbank-for-docker`
2. Откройте **Settings** (Настройки) в верхнем меню репозитория
3. В левом меню найдите раздел **Pages** (или **Pages** в разделе Code and automation)
4. В разделе **Source** (Источник) выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)` или `/docs` (если используется подпапка)
5. Нажмите **Save** (Сохранить)

### Шаг 2: Проверьте права доступа для GITHUB_TOKEN

GitHub Actions должен иметь права на запись в репозиторий:

1. Перейдите в **Settings → Actions → General**
2. Найдите раздел **Workflow permissions**
3. Убедитесь, что выбрано:
   - ✅ **Read and write permissions**
   - ✅ **Allow GitHub Actions to create and approve pull requests** (опционально)

### Шаг 3: Проверьте ветку gh-pages

После первого успешного запуска пайплайна:

1. Убедитесь, что ветка `gh-pages` создана
2. Проверьте, что в ней есть файлы отчетов
3. Структура должна быть примерно такой:
   ```
   gh-pages/
   ├── 266/
   │   ├── index.html (Allure отчет)
   │   ├── swagger-coverage-report.html
   │   └── videos/
   └── ...
   ```

### Шаг 4: Дождитесь публикации

После включения GitHub Pages:
- Первая публикация может занять **до 10 минут**
- Последующие обновления обычно занимают **1-2 минуты**
- Проверьте статус публикации в **Settings → Pages**

## Проверка

После настройки отчеты должны быть доступны по адресам:

- **Allure Report**: `https://01yura.github.io/nbank-for-docker/266/`
- **Swagger Coverage**: `https://01yura.github.io/nbank-for-docker/266/swagger-coverage-report.html`
- **Test Videos**: `https://01yura.github.io/nbank-for-docker/266/videos/`

Где `266` - это номер запуска пайплайна (`github.run_number`).

## Устранение неполадок

### Ошибка 404 после настройки

1. Подождите 5-10 минут после включения GitHub Pages
2. Проверьте, что ветка `gh-pages` существует и содержит файлы
3. Убедитесь, что в настройках Pages выбран правильный источник (`gh-pages` branch, root folder)

### Отчеты не обновляются

1. Проверьте логи шага "Publish test report" в GitHub Actions
2. Убедитесь, что шаг выполняется успешно
3. Проверьте права доступа для `GITHUB_TOKEN`

### Ветка gh-pages не создается

1. Проверьте логи шага "Publish test report"
2. Убедитесь, что `GITHUB_TOKEN` имеет права на запись
3. Попробуйте запустить пайплайн вручную через `workflow_dispatch`

## Дополнительная информация

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация peaceiris/actions-gh-pages](https://github.com/peaceiris/actions-gh-pages)
